apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: dev
  namespace: argocd
spec:
  description: Dev environment project
  sourceRepos:
    - '*'
  destinations:
    # Allow apps to deploy to the microk8s1 cluster
    - namespace: '*'
      server: https://192.168.4.95:16443
    # Allow root-level apps to deploy to the ArgoCD management cluster
    - namespace: argocd
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
